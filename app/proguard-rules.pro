# Add project specific ProGuard rules here.
# MVP ships with isMinifyEnabled = false in both build types (see app/build.gradle.kts),
# so this file is currently inert — kept as a placeholder for when release minification
# is turned on post-MVP.
