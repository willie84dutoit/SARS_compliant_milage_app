# ── Python ──────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/

# ── Secrets / credentials (NEVER commit) ────────────────
.env
.env.*
*.key
*.pem
serviceAccount*.json
google-services.json
GoogleService-Info.plist
*.keystore
*.jks

# ── Android ─────────────────────────────────────────────
*.iml
.gradle/
.kotlin/
local.properties
/build/
**/build/
.cxx/
captures/
# build artifacts (belt-and-braces alongside the build/ dirs above)
*.apk
*.aab
*.ap_
*.dex
*.hprof
output.json
release/

# ── iOS / Xcode ─────────────────────────────────────────
xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/

# ── Docker / Cloud ──────────────────────────────────────
*.local

# ── IDE / OS ────────────────────────────────────────────
.idea/
.vscode/
.DS_Store
Thumbs.db

# ── Local dev tooling runtime state ─────────────────────
scripts/.dev-pids.json

# ── Project internals: local only, not for the public repo ──────────
team/
userstories/
agile/

# ── Claude Code: keep agents/skills/memory, ignore local overrides ──
.claude/

# ── OCR-accuracy spike tooling (local only — stock-image licensing + repo bloat) ──
# Reusable scripts (download_odometers.py, confirm_readings.py) ARE committed; the
# downloaded images, generated data, and the spike instrumented test stay local.
tools/ocr-test/odometer_images/
tools/ocr-test/readings.csv
tools/ocr-test/confirmations.csv
tools/ocr-test/report.md
app/src/androidTest/assets/odometer_photos/
app/src/androidTest/kotlin/com/mileagetracker/app/ocr/OdometerOcrBatchTest.kt

